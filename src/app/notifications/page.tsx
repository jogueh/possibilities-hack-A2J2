"use client";

import { BellOutlined } from "@ant-design/icons";
import PlaceholderPage from "@/components/PlaceholderPage";

export default function NotificationsPage() {
  return (
    <PlaceholderPage
      title="Notifications"
      description="Likes, comments, and job updates would show up here. (Mockup placeholder — your notifications are all caught up.)"
      icon={<BellOutlined />}
    />
  );
}
