"use client";

import { TeamOutlined } from "@ant-design/icons";
import PlaceholderPage from "@/components/PlaceholderPage";

export default function NetworkPage() {
  return (
    <PlaceholderPage
      title="My Network"
      description="Your connections, invitations, and recommendations will show up here. (This is a mockup, so the network is currently empty.)"
      icon={<TeamOutlined />}
    />
  );
}
