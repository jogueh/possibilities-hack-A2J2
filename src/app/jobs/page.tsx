"use client";

import { ShopOutlined } from "@ant-design/icons";
import PlaceholderPage from "@/components/PlaceholderPage";

export default function JobsPage() {
  return (
    <PlaceholderPage
      title="Jobs"
      description="Job recommendations tailored to your profile will appear here. (Mockup placeholder — no real job listings.)"
      icon={<ShopOutlined />}
    />
  );
}
