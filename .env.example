# OpenRouter API key for goal parsing + talking-points generation.
# When unset, the LLM paths fall back to deterministic keyword extraction.
# Get one from https://openrouter.ai/keys
OPENROUTER_API_KEY=
